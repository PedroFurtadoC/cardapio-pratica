name: mobile
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.3

dependencies:
  cloud_firestore: ^5.5.0
  device_preview: ^1.3.1
  firebase_auth: ^5.4.0
  firebase_core: ^3.8.0
  flutter:
    sdk: flutter
  http: ^1.6.0
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
